Automatically creates vhost for apache
Also adds site to hosts
Usage example:

sudo ./vhost_creator.sh -u domain.com -d domain.com



Where:
 -u Domain for vhost and hosts
 -d Directory where site will be locate

